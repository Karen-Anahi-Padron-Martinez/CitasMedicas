export interface Cita {
    id: any;
    IdUsuario: string;  // ID del usuario que tiene la cita
    IdPsico: string;    // ID del psicólogo asignado
    FechaCita: string;  // Fecha de la cita en formato 'YYYY-MM-DD'
    HoraC: string;      // Hora de la cita en formato 'HH:MM'
  }
  